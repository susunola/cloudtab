# 2026-07-23

## Workspace Created
- Created standalone cloudtab workspace at `/Users/atom/WorkBuddy/cloudtab/`
- Copied from `/Users/atom/WorkBuddy/2026-07-21-11-06-52/cloudtab/` (full repo including .git)
- Initialized `.workbuddy/memory/` with MEMORY.md (build env, architecture, conventions)

## Architecture Refactoring — Extract Shared Parse Helpers
- `aws_common.go` 新增 `awsSimpleCost(name, raw)` 和 `awsScaledCost(name, count, raw)`，替代 7 个 AWS mapper 的重复 Parse 模板 (EC2/RDS/RDS Cluster/DocDB/Neptune/ElastiCache/Redshift/OpenSearch)
- `pricehelper.go` 新增 `parseTencentPrice(raw)` 统一处理 Price/Response 双层 JSON unwrap，替代 MySQL/PostgreSQL 的重复 unmarshal 块
- 净变更 +123/-134 行，`-race` 全过，无行为变更
- commit `ace495a`，已 push

## v0.2.1 Release
- Tagged `v0.2.1`, built 4 platform binaries (darwin/linux × amd64/arm64)
- Created GitHub Release via API: https://github.com/susunola/cloudtab/releases/tag/v0.2.1
- Uploaded 4 tarballs as release assets (HTTP 201 all)
- Updated MEMORY.md version → v0.2.1
